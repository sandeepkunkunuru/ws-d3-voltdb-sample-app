SQL: UPDATE BOOKS SET BOOK_ID = ?, BOOK_NAME = ? WHERE (BOOK_ID = ?);
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 LIMIT 1
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    UPDATE
     INDEX SCAN of "BOOKS" using its primary key index
      uniquely match (BOOK_ID = ?2), filter by (BOOK_ID = ?2)

