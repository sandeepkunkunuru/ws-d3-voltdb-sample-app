SQL: INSERT INTO books (book_name, book_id) VALUES (?, ?);
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 LIMIT 1
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    INSERT into "BOOKS"
     MATERIALIZE TUPLE from parameters and/or literals

